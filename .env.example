
# MetaComp API (set in deployment env; never commit the real key)
METACOMP_API_KEY=

# Shared secret required as the `x-proxy-secret` header on the /api/metacomp/*
# proxy routes. The proxy fails closed (refuses all requests) when this is unset.
PROXY_API_SECRET=
